GROUP BY QUESTIONS:
```

1. WAQTD NUMBER OF EMPLOYEES WORKING IN EACH DEPARTEMENT EXCEPT PRESIDENT

SELECT COUNT(*),DEPTNO
FROM EMP
WHERE JOB NOT IN 'PRESIDENT'
GROUP BY DEPTNO;
  COUNT(*)     DEPTNO
---------- ----------
         6         30
         5         20
         2         10

2.WAQTD TOTAL SALARY NEEDED TO PAY ALL THE EMPLOYEES IN EACH JOB 

SELECT SUM(SAL),JOB
FROM EMP
GROUP BY JOB;
  SUM(SAL) JOB
---------- ---------
      4150 CLERK
      5600 SALESMAN
      5000 PRESIDENT
      8275 MANAGER
      6000 ANALYST

3. WAQTD NUMBER OF EMPLOYEEES WORKING AS MANAGER IN EACH DEPARTMENT

SELECT COUNT(*),DEPTNO
FROM EMP
WHERE JOB='MANAGER'
GROUP BY DEPTNO;
  COUNT(*)     DEPTNO
---------- ----------
         1         30
         1         20
         1         10

4. WAQTD AVG SALARY NEEDED TO PAY ALL THE EMPLOYEES IN EACH DEPARTMENT EXCLUDING THE EMPLOYEES OF DEPTNO 20

SELECT AVG(SAL),DEPTNO
FROM EMP
WHERE DEPTNO NOT IN (20)
GROUP BY DEPTNO;
  AVG(SAL)     DEPTNO
---------- ----------
1566.66667         30
2916.66667         10

SELECT TRUNC(AVG(SAL)),DEPTNO
FROM EMP
WHERE DEPTNO NOT IN (20)
GROUP BY DEPTNO;
TRUNC(AVG(SAL))     DEPTNO
--------------- ----------
           1566         30
           2916         10


5. WAQTD NUMBER OF EMPLOYEES HAVING CHARACTER 'A' IN THEIR NAMES IN EACH JOB

SELECT COUNT(*),JOB
FROM EMP
WHERE ENAME LIKE '%A%'
GROUP BY JOB;
  COUNT(*) JOB
---------- ---------
         3 SALESMAN
         2 CLERK
         2 MANAGER

6. WAQTD NUMBER OF EMPLOYEES AND AVG SALARY NEEDED TO PAY THE EMPLOYEES WHO SALARY IN GREATER THAN 2000 IN EACH DEPT

SELECT COUNT(*),AVG(SAL),DEPTNO
FROM EMP
WHERE SAL>2000
GROUP BY DEPTNO;
  COUNT(*)   AVG(SAL)     DEPTNO
---------- ---------- ----------
         1       2850         30
         3 2991.66667         20
         2       3725         10

SELECT COUNT(*),TRUNC(AVG(SAL)),DEPTNO
FROM EMP
WHERE SAL>2000
GROUP BY DEPTNO;
  COUNT(*) TRUNC(AVG(SAL))     DEPTNO
---------- --------------- ----------
         1            2850         30
         3            2991         20
         2            3725         10

7. WAQDTD TOTAL SALARY NEEDED TO PAY AND NUMBER OF SALESMANS IN EACH DEPTNO

SELECT SUM(SAL),COUNT(*),DEPTNO
FROM EMP
GROUP BY DEPTNO;
  SUM(SAL)   COUNT(*)     DEPTNO
---------- ---------- ----------
      9400          6         30
     10875          5         20
      8750          3         10

8. WAQTD NUMBER OF EMPLOYEES WITH THEIR MAXIMUM SALARIES IN EACH JOB

SELECT COUNT(*),MAX(SAL),JOB
FROM EMP
GROUP BY JOB;
  COUNT(*)   MAX(SAL) JOB
---------- ---------- ---------
         4       1300 CLERK
         4       1600 SALESMAN
         1       5000 PRESIDENT
         3       2975 MANAGER
         2       3000 ANALYST

9. WAQTD MAXIMUM SALARIES GIVEN TO AN EMPLOYEE WORKING IN EACH DEPT

SELECT MAX(SAL),DEPTNO
FROM EMP
GROUP BY DEPTNO;
  MAX(SAL)     DEPTNO
---------- ----------
      2850         30
      3000         20
      5000         10

10. WAQTD NUMBER OF TIMES THE SALARIES HAVE BEEN REPEATED IN EMPLOYEE TABLE

SELECT COUNT(*),SAL
FROM EMP
GROUP BY SAL;
  COUNT(*)        SAL
---------- ----------
         1       2450
         1       5000
         1       1300
         2       1250
         1       2850
         1       2975
         1       1100
         2       3000
         1        800
         1       1600
         1       1500
         1        950

12 rows selected.


---------------------------------------------------------------------------
GROUP BY HAVING QUESTIONS:
```

1. WAQTD DNO AND NUMBER OF EMP WORKING IN EACH DEPT IF THERE ARE ATLEAST 2 CLERKS IN EACH DEPT

SELECT COUNT(*),DEPTNO
FROM EMP
WHERE JOB='CLERK'
GROUP BY DEPTNO
HAVING COUNT(JOB)>=2;
  COUNT(*)     DEPTNO
---------- ----------
         2         20

2.WAQTD DNO AND TOTAL SALARY NEEDED TO PAY ALL EMP IN EACH DEPT IF THERE ARE ATLEAST 4 EMP IN EACH DEPT

SELECT SUM(SAL),DEPTNO
FROM EMP
GROUP BY DEPTNO
HAVING COUNT(EMPNO)>=4;
  SUM(SAL)     DEPTNO
---------- ----------
      9400         30
     10875         20

3. WAQTD NUMBER OF EMP EARNING SAL MORE THAN 1200 IN EACH JOB AND THE TOTAL SAL NEEDED TO PAY EMP OF EACH JOB MUST EXCEEDS 3800

SELECT COUNT(*),SUM(SAL),JOB
FROM EMP
GROUP BY JOB
HAVING SUM(SAL)>3800;
  COUNT(*)   SUM(SAL) JOB
---------- ---------- ---------
         4       4150 CLERK
         4       5600 SALESMAN
         1       5000 PRESIDENT
         3       8275 MANAGER
         2       6000 ANALYST

4. WAQTD DEPTNO AND NUMBER OF EMP WORKING ONLY IF THERE ARE 2 EMP WORKING IN EACH DEPT AS MANAGER

SELECT COUNT(*),DEPTNO
FROM EMP
WHERE JOB='MANAGER'
GROUP BY DEPTNO
HAVING COUNT(*)=2;
no rows selected

5. WAQTD JOB AND MAX SAL OF EMP IN EACH JOB IF THE MAX SAL EXCEEDS 2600

SELECT JOB,MAX(SAL)
FROM EMP
GROUP BY JOB
HAVING MAX(SAL)>2600;
JOB         MAX(SAL)
--------- ----------
PRESIDENT       5000
MANAGER         2975
ANALYST         3000

6. WAQTD THE SALARIES WHICH ARE REPEATED IN EMP TABLE

SELECT SAL
FROM EMP
GROUP BY SAL
HAVING COUNT(SAL)>1;
    SAL
----------
      1250
      3000

7. WAQTD THE HIREDATE WHICH ARE DUPLICATED IN EMP TABLE

SELECT HIREDATE
FROM EMP
GROUP BY HIREDATE
HAVING COUNT(HIREDATE)>1;
HIREDATE
---------
03-DEC-81

8. WAQTD AVG SALARY OF EACH DEPT IF AVG SAL IS LESS THan 3000

SELECT TRUNC(AVG(SAL)),DEPTNO
FROM EMP
GROUP BY DEPTNO
HAVING TRUNC(AVG(SAL))<3000;
TRUNC(AVG(SAL))     DEPTNO
--------------- ----------
           1566         30
           2175         20
           2916         10

9. WAQTD DEPTNO IF THERE ARE ATLEAST 3 EMP IN EACH DEPT WHOSE NAME HAS CHAR 'A' OR 'S'

SELECT DEPTNO,COUNT(*)
FROM EMP
WHERE ENAME LIKE '%A%' OR ENAME LIKE '%S%'
GROUP BY DEPTNO
HAVING COUNT(*)>=3;
    DEPTNO   COUNT(*)
---------- ----------
        30          5
        20          4

10. WAQTD MIN AND MAX SALARIES OF EACH JOB IF MIN SAL IS MORE THAN 1000 AND MAX SAL IS LESS THAN 5000.

SELECT MIN(SAL),MAX(SAL),JOB
FROM EMP
GROUP BY JOB
HAVING MIN(SAL)>1000 AND MAX(SAL)<5000;
  MIN(SAL)   MAX(SAL) JOB
---------- ---------- ---------
      1250       1600 SALESMAN
      2450       2975 MANAGER
      3000       3000 ANALYST




11. WAQTD NUMBER OF EMPLOYEES HIRED ON THE SAME DAY INTO THE SAME DEPARTMENT

SELECT COUNT(*),DEPTNO,TO_CHAR(HIREDATE,'DAY')
FROM EMP
GROUP BY DEPTNO,TO_CHAR(HIREDATE,'DAY')
HAVING COUNT(*)>1;
  COUNT(*)     DEPTNO TO_CHAR(H
---------- ---------- ---------
         2         10 TUESDAY
         2         20 THURSDAY
         2         30 FRIDAY

12. WAQTD NUMBER OF EMPLOYEES GETTING THE SAME SALARY IN THE SAME DEPARTMENT

SELECT COUNT(*),SAL,DEPTNO
FROM EMP
GROUP BY SAL,DEPTNO
HAVING COUNT(*)>1;
  COUNT(*)        SAL     DEPTNO
---------- ---------- ----------
         2       1250         30
         2       3000         20

13.WAQTD NUMBER OF EMPLOYEES HAVING THE SAME NAME IN THE SAME DEPARTMENT

SELECT COUNT(*),ENAME,DEPTNO
FROM EMP
GROUP BY ENAME,DEPTNO
HAVING COUNT(*)>1;
no rows selected

14. WAQTD NUMBER OF EMPLOYEES GETTING THE SAME SALARY IN THE SAME DEPARTMENT AND HAVING THE SAME NAME

SELECT COUNT(*),SAL,DEPTNO,ENAME
FROM EMP
GROUP BY SAL,DEPTNO,ENAME
HAVING COUNT(*)>1;
no rows selected

15. WAQTD NUMBER OF EMPLOYEES GETTING THE SAME SALARY IN THE SAME DEPARTMENT AND HIRED ON THE SAME DAY.

SELECT COUNT(*),SAL,DEPTNO,HIREDATE
FROM EMP
GROUP BY SAL,DEPTNO,HIREDATE
HAVING COUNT(*)>1;
no rows selected

--------------------------------------------------------------------------------------